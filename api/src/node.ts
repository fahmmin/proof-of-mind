/** Node CLI / deploy entry — uses absolute zk asset paths. */
export { ProofOfMindAPI } from './index.js';
export {
  CompiledProofOfMindContract,
  ledger,
  pureCircuits,
  zkConfigPath,
} from '../../contracts/index.js';
